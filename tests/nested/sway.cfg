# Minimal sway config for the nested lock-readiness harness (issue #44).
# Headless backend, no bar, no xwayland, one predictable output.
xwayland disable
default_border none
output HEADLESS-1 resolution 1280x800
