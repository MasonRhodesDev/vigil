# rpmlint filters for vigil. Lets CI gate on rpmlint without tripping over
# domain vocabulary and our deliberate vendored-source layout.

# Domain terms rpmlint's dictionary doesn't know (greetd, compositor, KMS,
# lockscreen, Slint, xkb, tmpfiles, ...). Not real defects.
addFilter("spelling-error")

# Source1 is the cargo-vendor tarball produced by build-srpm.sh, not a URL —
# intentional (offline, reproducible RPM build).
addFilter("invalid-url Source1")

# Shared setgid directory so desktop users can edit layouts without root.
# hyprstate ships the same path and attributes.
addFilter("non-standard-dir-perm /etc/monitor-profiles")
addFilter("non-standard-gid /etc/monitor-profiles")
