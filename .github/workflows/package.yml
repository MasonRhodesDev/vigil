name: Package

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: package-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  arch-ci:
    uses: MasonRhodesDev/packaging-workflows/.github/workflows/rust-ci.yml@d85cbe2888dc868016c85452f7c339405b8528c1
    with:
      # smithay backends + xkbcommon + pam (bindgen needs clang)
      pacman-deps: seatd libinput libxkbcommon systemd libdrm mesa pam clang fontconfig ttf-dejavu

  arch:
    uses: MasonRhodesDev/packaging-workflows/.github/workflows/arch-package.yml@d85cbe2888dc868016c85452f7c339405b8528c1

  rpm:
    uses: MasonRhodesDev/packaging-workflows/.github/workflows/rpm-check.yml@d85cbe2888dc868016c85452f7c339405b8528c1
    with:
      spec-name: vigil
