name: Package

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: package-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  arch-ci:
    uses: MasonRhodesDev/packaging-workflows/.github/workflows/rust-ci.yml@89a6561a85e21ca8425dbdace40d115c0bbc852d
    with:
      # smithay backends + xkbcommon + pam (bindgen needs clang)
      pacman-deps: seatd libinput libxkbcommon systemd libdrm mesa pam clang fontconfig ttf-dejavu

  arch:
    uses: MasonRhodesDev/packaging-workflows/.github/workflows/arch-package.yml@89a6561a85e21ca8425dbdace40d115c0bbc852d

  rpm:
    uses: MasonRhodesDev/packaging-workflows/.github/workflows/rpm-check.yml@89a6561a85e21ca8425dbdace40d115c0bbc852d
    with:
      spec-name: vigil
