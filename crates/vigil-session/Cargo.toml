[package]
name = "vigil-session"
description = "libseat session glue (smithay) -> session events"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
vigil-core.workspace = true
smithay = { workspace = true, features = ["backend_session_libseat"] }
